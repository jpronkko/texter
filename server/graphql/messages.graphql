type GroupMessage {
  id: ID!
  fromUser: ID!
  group: ID!
  text: String!
  sentTime: String!
}

input MessageInput {
  groupId: ID!
  text: String!
}

type Query {
   findMessages(groupName: String!): [GroupMessage]
}

type Mutation {
  createMessage(MessageInput: MessageInput): GroupMessage
}