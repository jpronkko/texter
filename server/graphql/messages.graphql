type Message {
  id: ID!
  fromUser: ID!
  text: String!
  sentTime: String!
}

input MessageInput {
  groupId: ID!
  text: String!
}

type Query {
  allMessages: [Message]
  findMessages(groupId: ID!): [Message]
}

type Mutation {
  createMessage(MessageInput: MessageInput): Message
}