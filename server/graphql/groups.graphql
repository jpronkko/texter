type Group {
  id: ID!
  name: String!
  topics: [ID]
}

type GroupInfo {
  id: ID!
  ownerId: ID!
  name: String!
}

type Query {
  allGroups: [GroupInfo]
  getTopics(groupId: ID!): [Topic]
}

type Mutation {
  createGroup(name: String!): GroupInfo
}
