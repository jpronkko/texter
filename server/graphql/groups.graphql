type Group {
  id: ID!
  name: String!
  messages: [ID]
}

type GroupInfo {
  id: ID!
  ownerId: ID!
  name: String!
}

type Query {
  allGroups: [GroupInfo]
  getMessages(groupId: ID!): [MessageInfo]
}

type Mutation {
  createGroup(name: String!): GroupInfo
}
