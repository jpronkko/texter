type Group {
  id: ID!
  name: String!
  ownerId: ID!
  messages: [ID]
}

type Query {
  findGroups(ownerId: ID!): [Group],
  findGroup(ownerId: ID!, name: String!): Group
}

type Mutation {
  createGroup(name: String!): Group
}