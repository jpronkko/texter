type Group {
  id: ID!
  name: String!
  messages: [ID]
}

type GroupInfo {
  id: ID!
  name: String!
}

type Query {
  allGroups: [GroupInfo]
  getUserGroupInfo: [GroupInfo]
}

type Mutation {
  createGroup(name: String!): Group
}
