type Group {
  id: ID!
  name: String!
  ownerId: ID!
  messages: [ID]
}

input GroupInput {
  name: String!
  ownerId: ID!
}

type Query {
  findGroups(ownerId: ID!): [Group],
  findGroup(ownerId: ID!, name: String!): Group
}

type Mutation {
  createGroup(input: GroupInput): Group
}