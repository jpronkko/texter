type Topic {
  id: ID!
  groupId: ID!
  name: String!
  messages: [ID]
}

type Query {
  allTopics: [Topic]
  getMessages(topicId: ID!): [MessageInfo]
}

type Mutation {
  createTopic(groupId: ID!, name: String!): Topic
}

type Subscription {
  topicAdded(groupId: ID!): Invitation
}
